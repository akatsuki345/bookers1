<!DOCTYPE html>
<html>
    <body>
        <p id="notice"></p>
        <h1>Books</h1>
         <table>
             <thead>
                 <tr>
                     <th>Title</th>
                     <th>Body</th>
                     <th colspan="3"></th>
                 </tr>
             </thead>
    </body>
    <tbody>
        <% @books.each do |book| %>
        <span><%= link_to "Show", book_path(book.id) %></span>
        <span><%= link_to "Edit", book_path(book.id) %></span>
        <span><%= link_to "Destroy", book_path(book.id) %></span>
  　     <% end %>

    </table>
    <h1>New book</h1>
    <%= form_with model: @book, url: '/books', method: :post do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>

    <h4>Body</h4>
    <%= f.text_area :body %>

    <%= f.submit 'Create Book' %>
    <% end %>
</html>